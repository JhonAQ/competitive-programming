#include<bits/stdc++.h>
#define int long long

using namespace std;

signed main(){
  int n; cin >> n;
  int test = n;
  vector<string> predict;
  while(n--){
    string s; cin >> s;
    predict.push_back(s);
  }
  for(int i = 0; i < test; i++){
    string a; cin >> a;
    idx = vector.find(a);
  }
}
