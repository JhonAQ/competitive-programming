#include<bits/stdc++.h>

using namespace std;

bitset<500> isP;

int main(){
  isP.set();
  isP[1] = isP[0] = 0;
  for(int i = 2; i < 501; i++){
    if(isP[i]){
      for(int j = i * i; j)
    }
  }
}