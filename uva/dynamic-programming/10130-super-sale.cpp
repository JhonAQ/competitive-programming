#include<bits/stdc++.h>

using namespace std;

int maxP(int w, ){

}

vector<pair<int, int>> ob;
vector<int> g;

int main(){
  int tc; cin >> tc;
  while(tc--){
    int n; cin >> n;
    while(n--){
      int np, nw; cin >> np >> nw;
      ob.first = np;
      ob.second = nw;
    }
    int g; cin >> g;
    while(g--){
      int gw; cin >> gw;
    }

  }
}
