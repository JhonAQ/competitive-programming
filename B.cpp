#include<bits/stdc++.h>

using namespace std;

int main(){
  int tc; cin >> tc;
  while(tc--){
    int n, k; cin >> n >> k;
    int ans;
    bool needDes = false;
    while(n--){
      int p; cin >> p;
      if(!needDes && p == 0 ){
        acum ++
      }
      if(acum )
    }
  }
}
