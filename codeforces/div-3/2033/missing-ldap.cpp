#include<bits/stdc++.h>

using namespace std;

int main(){
  string str;
  getline(cin, str);
  stringstream ss(str);
  vector<string> vs;
  vector<string> names;
  string temp;
  while(ss>>temp){
    vs.push_back(temp);
  }

  int size = vs[0].size() * vs[3].size();
  for(int i = 0; i < sz)

}
